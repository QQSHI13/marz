{
  "name": "marz-golden-fixtures",
  "version": "0.0.0",
  "private": true,
  "description": "Generates the English ranking oracle in tests/fixtures from lunr.js. Not published, not part of the js/ package.",
  "scripts": {
    "generate": "node generate_golden.js"
  },
  "devDependencies": {
    "lunr": "2.3.9"
  }
}
